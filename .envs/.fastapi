# DB (для доступа к моделям Django из FastAPI)
POSTGRES_DB=app_db
POSTGRES_USER=app_user
POSTGRES_PASSWORD=app_password
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Django settings module (для инициализации Django внутри FastAPI)
DJANGO_SETTINGS_MODULE=django_project.settings

# Для логов
UDP_LOG_HOST=45.91.238.51
UDP_LOG_PORT=9999
SERVER_NAME=djangofastapi
